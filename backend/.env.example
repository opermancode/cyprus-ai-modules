DATABASE_URL = "postgresql://postgres:Sanghvi@admin123@localhost:5432/helpdesk"
EMAIL_SERVICE="gmail"
MAIL_FROM="bulkway.support@gmail.com"
EMAIL_PASSWORD="BulkWay@119"
RESEND_API_KEY=re_EsD9wptw_6zoYmpeTPoJfn89DHZMW

SMTP_HOST=smtp.office365.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=customerpulse
SMTP_PASS=
MAIL_FROM="Sanghvi Movers Ltd <cus"
APP_URL = https://

# ---- Cyprus AI assistant (self-hosted LLM via Ollama) ----
# LLM_BASE_URL must point at an OpenAI-compatible chat completions endpoint.
# Same server as the app:  http://localhost:11434/v1  (Ollama's OpenAI-compat
# base is on /v1). Separate server:  http://<host>:<port>/v1
LLM_BASE_URL=http://localhost:11434/v1
# Model to use for description refinement. Keep it small on CPU-only boxes.
LLM_MODEL=qwen2.5:1.5b

